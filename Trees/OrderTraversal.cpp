#include <iostream>
#include </home/nick/C++/Trees/Binary.cpp>

void InOrderTraversal(node_s * node){
    while(node->left != NULL)
    

}

int main(){
    bool found = false;
    int dele_item = -1;
    struct node * root = NULL;
    root = insert(root, 50);
    insert(root, 30);
    insert(root, 20);
    insert(root, 10);
    insert(root, 05);
    insert(root, -5);
    insert(root, 100);
    insert(root, 200);
    insert(root, 300);
    insert(root, 400);
    insert(root, 500);
    insert(root, 600)
    
    return 0;
}